/**
 * Phase 4 stubs — wire these in App.tsx / React Router later:
 * - Home
 * - Listings
 * - BusinessDetail
 * - Login / Register / Account
 */
export function PlaceholderPage({ title }: { title: string }) {
  return <main><h1>{title}</h1><p>Coming in Phase 4.</p></main>;
}
